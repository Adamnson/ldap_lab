#ifndef GLOBAL_STATS_H
#define GLOBAL_STATS_H

// Adds a message to the dynamic array
void special_notifier(const char *message);

// Prints all stored observations and frees the allocated memory
void display_special_observations(void);

#endif // GLOBAL_STATS_Hint special_notifier(a){
